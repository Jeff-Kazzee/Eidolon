{
  "name": "eidolon",
  "version": "0.1.0",
  "private": true,
  "description": "Desktop AI assistant with chat, writing studio, and image generation",
  "author": "Jeff Kazzee",
  "license": "MIT",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"bun run dev:shared\" \"bun run dev:desktop\" \"bun run dev:web\"",
    "dev:shared": "bun --cwd packages/shared run dev",
    "dev:desktop": "bun --cwd apps/desktop run dev",
    "dev:web": "bun --cwd apps/web run dev",
    "build": "bun run build:shared && bun run build:desktop && bun run build:web",
    "build:shared": "bun --cwd packages/shared run build",
    "build:desktop": "bun --cwd apps/desktop run build",
    "build:web": "bun --cwd apps/web run build",
    "lint": "bun run lint:shared && bun run lint:desktop && bun run lint:web",
    "lint:shared": "bun --cwd packages/shared run lint",
    "lint:desktop": "bun --cwd apps/desktop run lint",
    "lint:web": "bun --cwd apps/web run lint",
    "typecheck": "tsc -b",
    "test": "bun run test:shared && bun run test:desktop && bun run test:web",
    "test:shared": "bun --cwd packages/shared run test",
    "test:desktop": "bun --cwd apps/desktop run test",
    "test:web": "bun --cwd apps/web run test",
    "clean": "bun --cwd packages/shared run clean && bun --cwd apps/desktop run clean && bun --cwd apps/web run clean",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,jsx,json,md}\""
  },
  "devDependencies": {
    "@types/bun": "^1.1.0",
    "prettier": "^3.4.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "packageManager": "bun@1.1.0"
}
